// @flow
import React, { PropTypes } from 'react';

const AppMenu = (props: Object) => {
  console.log('appMenu', props);
  return (
    <div>
      <h1>This is the App Menu</h1>
    </div>
  );
};

export default AppMenu;
