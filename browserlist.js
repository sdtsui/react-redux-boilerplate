// @flow
/**
 * Used by autoprefixer and stylelint
 * @type {[*]}
 */
const browsers:[string] = [
  'Chrome >= 25',
  'Firefox >= 15',
  'IE >= 9',
  'iOS >= 5',
  'Safari >= 7',
];

module.exports = browsers;
